# ML Analytics System - Примеры Использования

## Содержание
1. [Dashboard Commands](#dashboard-commands)
2. [Transcript Commands](#transcript-commands)
3. [Dev Messages](#dev-messages)
4. [Reports](#reports)
5. [API Examples](#api-examples)

---

## Dashboard Commands

### Команда /dashboard

**Для операторов** (видят только свой дашборд):
```
Пользователь: /dashboard
Бот: [Меню с кнопками]
      👤 Мой дашборд

Пользователь: [Нажимает "Мой дашборд"]
Бот: 
📊 Live Dashboard - Иванова А.И.
📅 Период: День

1️⃣ Общая статистика:
   • Всего звонков: 45
   • Записей: 18
   • Конверсия: 40%

2️⃣ Качество:
   • Средняя оценка: 8.2/10
   • Оценка лидов: 8.5/10

[Кнопки: День ◉ | Неделя ○ | Месяц ○]
[🔄 Обновить]
```

**Для админов** (видят всех):
```
Пользователь: /dashboard
Бот: [Меню с кнопками]
      📊 Сводный дашборд
      👤 Мой дашборд
      🔍 Выбрать оператора

Пользователь: [Нажимает "Сводный дашборд"]
Бот:
📊 Сводный Dashboard
📅 Период: День

🔥 Петрова М.В.
   Звонков: 52 | Записей: 24 | Конверсия: 46% | Оценка: 8.9/10

✅ Иванова А.И.
   Звонков: 45 | Записей: 18 | Конверсия: 40% | Оценка: 8.2/10

⚠️ Сидоров И.П.
   Звонков: 38 | Записей: 8 | Конверсия: 21% | Оценка: 7.5/10
```

---

## Transcript Commands

### Команда /transcript <call_id>

```
Пользователь: /transcript 12345
Бот:
📞 Расшифровка звонка #12345

🆔 History ID: 67890
📅 Дата: 2024-12-04 14:23
📱 Телефон: +790*****12
📞 Принял: 1234
⏱ Длительность: 4:30
⭐️ Оценка: 8.5/10
📂 Категория: Запись на услугу (успешная)
🎯 Результат: record

━━━━━━━━━━━━━━━━━━━━

📝 Транскрипт:

Оператор: Здравствуйте, клиника "Здоровье", меня зовут Анна, чем могу помочь?
Клиент: Здравствуйте, хотел бы записаться на прием к терапевту...
[полный текст разговора]
```

**Ограничения доступа:**
- Операторы видят только свои звонки
- Админы видят все звонки
- При попытке просмотра чужого звонка:
  ```
  🔒 У вас нет прав для просмотра этого звонка.
  Вы можете просматривать только свои звонки.
  ```

---

## Dev Messages

### Команда /message_dev

**Отправка сообщения:**
```
Пользователь: /message_dev
Бот: 
📨 Отправка сообщения разработчику

Введите ваше сообщение. Оно будет отправлено всем разработчикам.
Отправьте /cancel для отмены.

Пользователь: Не работает кнопка "Обновить" в дашборде
Бот:
✅ Ваше сообщение отправлено 2 разработчикам.
Они свяжутся с вами в ближайшее время.
```

**Разработчик получает:**
```
📨 Новое сообщение

От: Иванова Анна
Username: @ivanova_ai
Оператор: Иванова А.И.
Telegram ID: 123456789

━━━━━━━━━━━━━━━━━━━━

Не работает кнопка "Обновить" в дашборде

[Кнопка: ✉️ Ответить]
```

**Ответ разработчика:**
```
Dev: [Нажимает "Ответить"]
Bot: ✉️ Ответ пользователю
     Введите ваш ответ...

Dev: Спасибо за сообщение! Поправили, попробуйте обновить через 5 минут
Bot: ✅ Ваш ответ отправлен пользователю.

---

Пользователь получает:
📬 Ответ от Разработчика

От: Алексей Смирнов

━━━━━━━━━━━━━━━━━━━━

Спасибо за сообщение! Поправили, попробуйте обновить через 5 минут
```

---

## Reports

### Команда /report

**Для операторов:**
```
Пользователь: /report daily
Бот: Генерация отчета (daily)...

📊 Отчет для оператора: Иванова А.И.
📅 Период: 04.12.2024 - 04.12.2024

1️⃣ Общая статистика:
   • Всего звонков: 45
   • Записей: 18
   • Конверсия: 40%

2️⃣ Качество:
   • Средняя оценка: 8.2/10
   • Оценка лидов: 8.5/10

3️⃣ Время:
   • Среднее время разговора: 4:30

4️⃣ Проблемы:
   • Отмен/переносов: 3
   • Жалоб: 1

💡 Рекомендации:

📉 Низкая конверсия в запись (40%)
- Больше узнавайте о потребностях клиента
- Используйте технику "Да-да-да"
- Не торопитесь с записью, сначала создайте ценность

✅ Работа на хорошем уровне
- Отличное качество обслуживания (8.5/10)
- Продолжайте в том же духе
```

**Для админов:**
```
Пользователь: /report
Бот: 
Выберите оператора для генерации отчета.
Показано 1-8 из 15.

[Кнопки с операторами:]
Иванова А.И. (1234)
Петрова М.В. (1235)
Сидоров И.П. (1236)
...

[⬅️ Назад] [➡️ Далее]

Пользователь: [Выбирает оператора]
Бот: [Генерирует отчет для выбранного]
```

---

## API Examples

### Python - Получение Dashboard Метрик

```python
from app.db.manager import DatabaseManager
from app.db.repositories.analytics import AnalyticsRepository
from datetime import date

# Инициализация
db_manager = DatabaseManager()
analytics = AnalyticsRepository(db_manager)

# Получить дашборд для оператора
dashboard = await analytics.get_live_dashboard_single(
    operator_name="Иванова А.И.",
    period_type='day'
)

print(f"Звонков: {dashboard['accepted_calls']}")
print(f"Записей: {dashboard['records_count']}")
print(f"Конверсия: {dashboard['conversion_rate']}%")
```

### Python - Генерация Рекомендаций

```python
from app.services.recommendations import RecommendationsService
from app.db.repositories.analytics import AnalyticsRepository
from datetime import date

# Получить проблемные звонки
calls = await analytics.get_calls_for_recommendations(
    operator_name="Иванова А.И.",
    date_from=date(2024, 12, 4),
    date_to=date(2024, 12, 4),
    limit=10
)

# Генерировать рекомендации
recs_service = RecommendationsService()
recommendations = await recs_service.generate_operator_recommendations(
    operator_name="Иванова А.И.",
    calls_data=calls,
    stats={'conversion_rate': 40, 'avg_score_all': 8.2}
)

print(recommendations)
```

### Python - Работа с Кешем

```python
from app.services.dashboard_cache import DashboardCacheService

cache = DashboardCacheService(db_manager)

# Попробовать получить из кеша
cached = await cache.get_cached_dashboard(
    operator_name="Иванова А.И.",
    period_type='day',
    period_start=date.today(),
    period_end=date.today()
)

if cached:
    print("Используем кеш!")
    dashboard = cached
else:
    # Получить свежие данные
    dashboard = await analytics.get_live_dashboard_single(...)
    # Сохранить в кеш
    await cache.save_dashboard_cache(dashboard)
```

### Python - Проверка Прав

```python
from app.services.permissions import PermissionChecker

checker = PermissionChecker(db_manager)

# Проверить может ли видеть всю статистику
can_view = await checker.can_view_all_stats(telegram_id=123456)

if can_view:
    # Показать сводный дашборд
    dashboards = await analytics.get_live_dashboard_all_operators('day')
else:
    # Показать только свой
    dashboard = await analytics.get_live_dashboard_single(...)
```

### SQL - Прямые Запросы

**Получить топ операторов по конверсии:**
```sql
SELECT 
    operator_name,
    conversion_rate,
    accepted_calls,
    records_count
FROM operator_dashboards
WHERE period_type = 'day'
  AND period_start = CURDATE()
ORDER BY conversion_rate DESC
LIMIT 10;
```

**Получить операторов с низкой оценкой:**
```sql
SELECT 
    operator_name,
    avg_score_all,
    complaint_calls
FROM operator_dashboards
WHERE period_type = 'day'
  AND period_start = CURDATE()
  AND avg_score_all < 7
ORDER BY avg_score_all ASC;
```

---

## Workflow Examples

### Ежедневный Workflow Оператора

1. **Утро - проверка дашборда:**
   ```
   /dashboard → День → Проверить конверсию и оценку
   ```

2. **Середина дня - проверка проблемных звонков:**
   ```
   Если конверсия < 40% → посмотреть звонки через /transcript
   ```

3. **Вечер - получение отчета:**
   ```
   /report daily → Прочитать рекомендации
   ```

### Workflow Администратора

1. **Мониторинг команды:**
   ```
   /dashboard → Сводный → День
   Выявить проблемных операторов (🔴 красные)
   ```

2. **Детальный анализ:**
   ```
   /dashboard → Выбрать оператора → Посмотреть детали
   ```

3. **Генерация отчетов:**
   ```
   /report → Выбрать оператора → Проверить рекомендации
   ```

4. **Обратная связь:**
   ```
   Связаться с оператором напрямую или через систему
   ```

---

## Tips & Best Practices

### Для Операторов

1. **Проверяйте дашборд 2-3 раза в день**
   - Утро: планирование дня
   - Обед: коррекция подхода
   - Вечер: анализ результатов

2. **Обращайте внимание на рекомендации**
   - Они генерируются на основе реальных звонков
   - Применяйте советы сразу же

3. **Используйте /transcript для учебы**
   - Слушайте свои лучшие звонки (оценка > 9)
   - Анализируйте проблемные (оценка < 6)

### Для Администраторов

1. **Мониторьте тренды, а не моментальные показатели**
   - Смотрите неделю/месяц для оценки динамики
   - День может быть аномальным

2. **Используйте сводный дашборд для быстрой оценки**
   - Цветные индикаторы (🔥✅⚠️❌) помогают быстро найти проблемы
   - Топ-10 показывает лучших и худших

3. **Персонализируйте обратную связь**
   - Используйте конкретные примеры из звонков
   - Ссылайтесь на метрики и рекомендации системы

---

**Документация обновлена:** 04.12.2024
