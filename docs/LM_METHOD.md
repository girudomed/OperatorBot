# LM-методология Operabot

Документ фиксирует, что такое LM-аналитика в проекте, какие данные входят в расчёт, какие фильтры применяются и от каких условий зависит надёжность вывода. Используйте его, когда нужно объяснить результат, согласовать изменения или подключить ML.

## 1. Что такое LM

LM (Logic Model) — аналитический слой поверх операционных данных. Он не дублирует дашборд, а отвечает на вопросы «почему так» и «что сделать дальше». Основные сущности:

| Метрика | Смысл | Где смотреть |
| --- | --- | --- |
| `lost_opportunity_score` | Насколько болезненно упущен целевой звонок | списки 💸 Потери, карточка звонка |
| `followup_needed_flag` | Требуется ли контролируемый перезвон | списки 📞 «Нужно перезвонить» |
| `complaint_risk_flag` / `complaint_prob` | Вероятность эскалации жалобы | списки ⚠️ Жалобы, светофор |
| `churn_risk_level` | Риск оттока клиента | списки 📉 Отток |
| `conversion_score`, `normalized_call_score`, `script_risk_index` | Интерпретация качества разговора | светофор / карточка |
| `lm_version_tag`, `calc_profile` | Метаданные расчёта | карточка звонка |

LM «видит» только принятые целевые звонки (`is_target=1`, `talk_duration ≥ 15 c`, есть живой диалог). Всё остальное (спам, автоответчик, информационные запросы) фильтруется ещё до построения сигналов.

## 2. Что *не* считается

| Категория | Правило исключения |
| --- | --- |
| Спам/реклама | `call_category` ∈ {`Спам`, `Реклама`, `Автоинформатор`} |
| Автоответчики, роботы | ключи `FOLLOWUP_AUTO_RESPONSES`, отсутствие живого диалога |
| Технические сбои | `call_type` содержит «сбой», либо в транскрипте ключи из `TECHNICAL_CALL_KEYWORDS` |
| Отсутствие услуги | `refusal_category_code = SERVICE_NOT_PROVIDED` или AI/транскрипт содержит фразы «не делаем» |
| Короткие разговоры | `talk_duration < 15 c` или <2 реплик (жалобы) / <10 c (флаг «Нужно перезвонить») |
| Исходы `record`, `spam`, `cancelled*` (без override) | флаг «Нужно перезвонить» не ставится, если исход закрывает процесс |

Любая метрика LM на таких звонках принудительно обнуляется: жалобы и флаг «Нужно перезвонить» не ставятся, потеря считается 0, churn остаётся LOW.

## 3. Фильтры и правила

### 3.1 Жалобы

Гейты в `LMService._calculate_complaint_risk`:

1. Kороткий разговор (`talk_duration < 15 c`) или <2 реплик → «нет диалога».
2. Категория «Сбой», технический звонок, отсутствие услуги → вывод «не считаем жалобой».
3. Должна выполняться комбинация **эмоциональный негатив** + **ответственность оператора/клиники** (ключи из `COMPLAINT_IRRITATION_KEYWORDS`, `COMPLAINT_EXPECTATION_KEYWORDS`, `COMPLAINT_THREAT_KEYWORDS`). Без связки сигнал = 0.

### 3.2 «Нужно перезвонить» (`followup_needed_flag`)

`_calculate_followup_needed` формирует контекст с причинами и SLA:

- Разрешающие коды (TIME_GENERAL, OPERATOR_WILL_CLARIFY и др.) берутся из `FOLLOWUP_ALLOWED_CODES`.
- Отказы SERVICE_NOT_PROVIDED, AGE_RESTRICTION, DOCUMENTS_REQUIRED снимают пометку «Нужно перезвонить».
- Нужен живой диалог ≥10 c и целевой исход (`is_target=1` или категория «Лид»).
- В `value_json` сохраняется `reason` + `sla_hours` (по дефолту 24). Карточка и списки показывают текст «почему» и срок.

### 3.3 Потери

`lost_opportunity_score` активируется только когда:

- `is_target=1` **и** `outcome = 'lead_no_record'`.
- Категории типа «Отмена», «Информационный» больше не попадают в потери.
- Бонусные баллы: разговор ≥30 c, `call_score ≤4`, нет заполненной причины отказа.

### 3.4 Прочие фильтры

- `MIN_SAMPLE_SIZE = 30` — если выборка меньше, LM помечает индикаторы как «недостаточно данных».
- Качество данных (покрытие `refusal`, `operator`, `utm`) выводится отдельно; при `refusal < 60%` явно предупреждаем, что аналитика потерь неточна.

## 4. Пороги и статусы

В `app/services/lm_rules.py::METRIC_CONFIG` задаются пороги светофора:

| Метрика | Зелёный | Жёлтый | Красный | Действие |
| --- | --- | --- | --- | --- |
| `complaint_risk_flag` | <30 | 30–59 | ≥60 | Открыть список жалоб, связаться ≤24 ч |
| `followup_needed_flag` | <20% | 20–39% | ≥40% (доля) | Пройтись по списку «Нужно перезвонить» |
| `lost_opportunity_score` | <40 | 40–59 | ≥60 | Разобрать причину отказа, вернуть в воронку |
| `normalized_call_score` | ≥75 | 60–74 | <60 | Разбор качества по чек-листу |

Для `churn_risk_level` текстовый статус (`LOW/MEDIUM/HIGH/CRITICAL`) задаётся в `_calculate_churn_risk`.

## 5. Надёжность вывода

LM-результаты считаются опорными, если выполняются условия:

1. **Выборка ≥ 30 звонков** (`MIN_SAMPLE_SIZE`). Иначе индикаторы → «не выявлено».
2. **Покрытие ключевых полей**: причина отказа ≥60%, оператор ≥80%. Блок «КАЧЕСТВО ДАННЫХ» подсвечивает, если цифры ниже.
3. **Нет технических ограничений**: свежие транскрипты, нет массовых «SERVICE_NOT_PROVIDED».
4. **Списки синхронизированы**: `action_counts` берутся из LMRepository и соответствуют реальному количеству карточек.

Если хотя бы один пункт не выполняется, LM всё равно работает, но на экране оператор сразу видит предупреждение, и вывод следует трактовать как ориентир, а не жёсткий KPI.

## 6. Как использовать документ

- **Руководители** — смотрят LM, чтобы понимать драйверы и списки действий. Споры вида «почему жалоб 0» закрываются ссылкой на этот документ: либо фильтр их снял (технический звонок), либо выборка мала.
- **Разработчики** — перед изменением порогов/фильтров обновляют этот файл и `lm_rules.py`. Новые ML-модели должны уважать базовые гейты и пороги.
- **Аналитики/ML** — могут безопасно строить модели поверх LM, зная, какие данные очищены и как классифицируются кейсы.

> Если добавляете новую метрику или меняете правила — обновите разделы 3–5, чтобы методология оставалась единой точкой правды.
