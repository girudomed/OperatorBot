# Интеграция Watch_dog с Системой Обработки Ошибок

## ✅ Статус: Полная интеграция

Вся система обработки ошибок теперь использует централизованное логирование через **watch_dog**.

## 🔄 Что было изменено

### 1. `app/utils/error_handlers.py`

**Было:**
```python
import logging
logger = logging.getLogger(__name__)
```

**Стало:**
```python
from watch_dog import get_watchdog_logger
logger = get_watchdog_logger(__name__)
```

### 2. `ErrorContext` класс

**Было:**
```python
self.logger = logging.getLogger(__name__)
```

**Стало:**
```python
self.logger = get_watchdog_logger(__name__)
```

## 🎯 Преимущества интеграции

### 1. Централизованное логирование
Все ошибки теперь идут через единую систему watch_dog, что обеспечивает:
- ✅ Единый формат логов
- ✅ Автоматическая маскировка секретов
- ✅ Централизованная ротация файлов
- ✅ Единая конфигурация уровней логирования

### 2. Маскировка секретов
Watch_dog автоматически маскирует чувствительные данные во всех логах ошибок:
- Пароли
- API ключи
- Токены
- Номера телефонов

### 3. Структурированное логирование
Все ошибки логируются с дополнительными полями через `extra`:
```python
logger.error(
    "Ошибка в функции",
    exc_info=True,
    extra={
        "function": func.__name__,
        "module": func.__module__,
        "exception_type": type(e).__name__,
        "exception_message": str(e)
    }
)
```

## 📊 Поток логирования

```
Исключение
    ↓
error_handlers.py (перехват)
    ↓
get_watchdog_logger (форматирование)
    ↓
watch_dog/filters.py (маскировка секретов)
    ↓
Файл логов + Console
```

## 🔍 Примеры логов

### До интеграции
```
ERROR - ValueError: Invalid user ID
```

### После интеграции
```
2024-12-02 12:49:00 [ERROR] app.services.reports - Ошибка в async app.services.reports.generate_report
Traceback (most recent call last):
  File "app/services/reports.py", line 50, in generate_report
    metrics = await self.metrics_service.calculate_operator_metrics(...)
ValueError: Invalid user ID

Extra:
  function: generate_report
  module: app.services.reports
  exception_type: ValueError
  exception_message: Invalid user ID
  
[СЕКРЕТЫ ЗАМАСКИРОВАНЫ]
```

## 🎨 Конфигурация watch_dog

Watch_dog настраивается через `watch_dog/config.py`:

```python
# Уровень логирования
LOG_LEVEL = "INFO"

# Формат логов
LOG_FORMAT = "%(asctime)s [%(levelname)s] %(name)s - %(message)s"

# Маскируемые паттерны
REDACT_PATTERNS = [
    "password",
    "token",
    "api_key",
    # ... и другие
]
```

## 🚀 Использование

Теперь все декораторы и утилиты автоматически используют watch_dog:

```python
from app.utils.error_handlers import log_async_exceptions

@log_async_exceptions  # ← Автоматически логирует через watch_dog
async def my_service_method(self, user_id):
    return await self.repo.get(user_id)
```

## ✅ Проверка интеграции

1. **Компиляция**: ✅ Успешно
2. **Импорты**: ✅ `get_watchdog_logger` доступен
3. **Логирование**: ✅ Все идет через watch_dog
4. **Маскировка**: ✅ Секреты скрыты

## 📝 Команды для проверки

```bash
# Поиск ошибок в логах (с маскировкой секретов)
grep ERROR logs/app.log

# Проверка работы watch_dog
tail -f logs/app.log

# Проверка маскировки
grep "password\|token\|api_key" logs/app.log
# Должны быть замаскированы как ****
```

## 🎯 Итог

Вся система обработки ошибок теперь **полностью интегрирована** с централизованным логированием watch_dog:

- ✅ Глобальные обработчики исключений → watch_dog
- ✅ Декораторы `@log_async_exceptions` → watch_dog  
- ✅ Контекстные менеджеры `ErrorContext` → watch_dog
- ✅ Все утилиты (safe_execute, format_exception_details) → watch_dog

**Результат**: Единая, централизованная система логирования с автоматической маскировкой секретов для всего приложения.
