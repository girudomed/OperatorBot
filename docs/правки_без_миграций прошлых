Ниже — что именно нужно поправить в логике кода, исходя из реальной схемы БД и тех миграций, которые фактически не были применены. Без кода, только список задач.

⸻

A. Роли и доступы (users vs UsersTelegaBot)
	1.	Во всех местах, где логика прав доступа завязана на таблицу users:
	•	убрать ожидание колонок role_id, status, approved_by, blocked_at, operator_id в users;
	•	явно переключить всю авторизационную/рольовую логику на таблицу UsersTelegaBot.
	2.	Модель/репозиторий, который сейчас:
	•	читает role_id и status из users,
	•	определяет «это оператор / админ / супер-админ»,
должен быть переделан так, чтобы:
	•	брать role_id, status, approved_by, blocked_at из UsersTelegaBot;
	•	использовать roles_reference как справочник ролей и прав (а не хардкодить права по числам в коде).
	3.	Все проверки прав вида «может ли пользователь…»:
	•	вместо «if user.role_id == …» делать:
	•	загрузка UsersTelegaBot.role_id по chat_id/user_id Telegram;
	•	проверка прав по roles_reference (can_view_own_stats, can_view_all_stats, can_manage_users, can_debug и т.д.).
	4.	Логика одобрения/блокировки пользователей бота:
	•	должна работать только с UsersTelegaBot.status (pending/approved/blocked);
	•	approved_by и blocked_at — тоже в UsersTelegaBot, а не в users;
	•	любые ссылки «кто одобрил» должны использовать UsersTelegaBot.id (или UsersTelegaBot.user_id в терминах телеграм-юзера).

⸻

B. Логи действий админов (admin_action_logs)
	5.	Во всех местах, где:
	•	записывается лог действия администратора в таблицу admin_action_logs,
	•	или читаются логи для аудита,
нужно:
	•	убедиться, что поле actor_id трактуется как UsersTelegaBot.id (ID пользователя бота), а не как users.id;
	•	поле target_id — тоже UsersTelegaBot.id (над кем выполнено действие);
	•	если в коде ещё зашито «FK на users.id» — удалить эти предположения, везде переходить на идентификаторы из UsersTelegaBot.
	6.	Все места, где формируется текст логов (approve/decline/promote/demote/block/unblock/lookup):
	•	должны передавать в admin_action_logs идентификаторы именно телеграм-пользователей (бот-пользователей), а не «внутренних» пользователей Mango.

⸻

C. Привязка Telegram-пользователя к оператору (операторская аналитика)
	7.	В логике «показать мою статистику» для оператора:
	•	перестать опираться на абстрактное user_id Mango;
	•	использовать один канонический идентификатор оператора, который вы выбрали:
	•	либо UsersTelegaBot.extension (например, 110, 101, 103, …) и далее джоин по users.extension или call_scores.called_info/caller_info,
	•	либо UsersTelegaBot.operator_name и далее сопоставление с call_scores.caller_info/called_info.
	8.	В репозиториях/сервисах, которые считают статистику по оператору:
	•	убрать случаи, где оператор определяется через «сырой» текстовый поиск по caller_info/called_info из Telegram-ника;
	•	добавить шаг:
	1.	найти запись в UsersTelegaBot по chat_id/user_id Telegram;
	2.	взять из неё operator_name и/или extension;
	3.	дальше использовать это значение в запросах к call_scores / call_analytics.
	9.	В логике, где строится список доступных операторов для выбора:
	•	не строить его напрямую из call_scores;
	•	сначала брать список актуальных операторов из users (Mango) или из UsersTelegaBot (если ведёте там нормализованные имена);
	•	только потом, при расчёте статистики, использовать их для фильтрации звонков.

⸻

D. Аналитика и дашборды (call_scores vs call_analytics)
	10.	Во всех местах, где код:
	•	ожидает, что в call_scores есть поля ml_p_record, ml_score_pred, ml_p_complaint, ml_updated_at,
	•	или рассчитывает/записывает туда ML-прогнозы,
нужно:
	•	убрать зависимость от этих полей в call_scores (их нет и не будет);
	•	перенести хранение ML-полей и вспомогательных метрик в таблицу call_analytics:
	•	чтение: брать ml_* из call_analytics по history_id или call_scores_id;
	•	запись: при пересчёте прогнозов обновлять call_analytics, а не call_scores.
	11.	В репозиториях/сервисах, которые строят тяжёлые выборки по звонкам (для дашбордов, отчётов, аналитики):
	•	проверить, не висят ли они всё ещё на «толстой» таблице call_scores с кучей фильтров;
	•	по возможности перевести их на call_analytics, где:
	•	денормализованы ключевые поля (operator_name, operator_extension, is_target, outcome, call_category, call_score, talk_duration),
	•	есть специализированные индексы под аналитику.
	12.	В логике формирования «дашборда по всем операторам» и «по конкретному оператору»:
	•	явным образом решить:
	•	либо всё читаем на лету из call_analytics с GROUP BY operator_name / extension;
	•	либо читаем из кеша operator_dashboards (если реализуете кеширование);
	•	скорректировать репозитории так, чтобы:
	•	запросы для телеграм-команд дашборда работали с выбранной таблицей (call_analytics или operator_dashboards), а не с call_scores.

⸻

E. Таблица кеша дашбордов (operator_dashboards)
	13.	Если код уже создан под работу с operator_dashboards (по изначальным миграциям ИИ), но таблица была изменена/пересоздана:
	•	проверить соответствие:
	•	используемых в коде полей (total_calls, accepted_calls, missed_calls, records_count, conversion_rate, avg_score_all, …) фактической схеме таблицы;
	•	в репозиториях/сервисах:
	•	нормализовать интерфейс: методы типа «get_dashboard_for_operator(period_type, date, operator_name)» должны работать с реальными колонками.
	14.	Если кеширование ещё не реализовано, но код предполагает его существование:
	•	временно вернуть вычисление метрик «на лету» из call_analytics;
	•	либо дописать периодический job (cron/команду бота), который:
	•	обходит операторов,
	•	считает агрегаты по call_analytics,
	•	записывает их в operator_dashboards.

⸻

F. Рекомендации по операторам (operator_recommendations + reports)
	15.	В коде, который генерирует текстовые рекомендации для конкретного оператора:
	•	разделить роли таблиц:
	•	operator_recommendations — хранит рекомендацию по оператору на дату;
	•	reports — хранит общие отчёты/сводки по периодам (в том числе по всей клинике/отделу);
	•	в логике бота:
	•	для команды вида «Рекомендации для оператора X за период Y» — читать из operator_recommendations;
	•	для команды «Отчёт по клинике/отделу за период» — читать из reports.
	16.	В тех местах, где раньше в reports.recommendations пытались складывать всё подряд (и по операторам, и по отделу):
	•	разграничить:
	•	оставить в reports.recommendations только «общие» рекомендации по периоду;
	•	индивидуальные по оператору — переносить и читать из operator_recommendations.

⸻

G. Админ-панель и хендлеры Telegram
	17.	В хендлерах админ-панели (AdminPanelHandler, AdminStatsHandler и аналоги):
	•	перепроверить все запросы к БД:
	•	если логика предполагает, что колонка/таблица «есть по миграциям ИИ», сравнить с фактической схемой;
	•	убрать обращения к несуществующим полям в users и call_scores;
	•	заменить на:
	•	UsersTelegaBot.* для прав/ролей/статусов;
	•	call_analytics / operator_dashboards для статистики.
	18.	В командах:
	•	/my_stats, /operator_stats, /all_operators, /operator_dashboard, /all_dashboard:
	•	убедиться, что:
	•	текущий Telegram-пользователь мапится через UsersTelegaBot;
	•	оператор определяется через operator_name/extension из UsersTelegaBot;
	•	далее статистика считается по call_analytics или берётся из operator_dashboards.
	19.	В командах управления пользователями бота:
	•	/approve_user, /block_user, /unblock_user, /set_role, /whoami, /list_users:
	•	все операции должны выполняться над UsersTelegaBot;
	•	все проверки права «может ли менять роли/блокировать» должны идти через roles_reference и UsersTelegaBot.role_id.

⸻



Ок, идём по варианту 2 — «большая архитектура» с аналитическим слоем и кешами.
Считай, что таблицы в БД уже есть, задача сейчас — вернуть/дописать логику в коде.

Ниже — по шагам, что именно надо сделать в коде, без SQL и без конкретных строк кода.

⸻

Шаг 0. Зафиксировать «канонический» идентификатор оператора

Принять решение и дальше везде использовать один вариант:
	•	либо UsersTelegaBot.extension → через него искать звонки (связка с users.extension, call_scores.called_info/caller_info);
	•	либо UsersTelegaBot.operator_name → совпадает с нормализованным именем в call_analytics.operator_name.

Дальше все репозитории и сервисы строить вокруг этого выбора.

⸻

Шаг 1. Роли и права через roles_reference

Файлы: репозитории пользователей/ролей, все хелперы проверки прав, хендлеры /admin, /make_admin, /make_superadmin, /approve и т.п.
	1.	Добавить/вернуть RolesRepository/RoleService, который:
	•	читает roles_reference по role_id;
	•	возвращает флаги: can_view_own_stats, can_view_all_stats, can_manage_users, can_debug и т.д.
	2.	Во всех местах, где сейчас:
	•	стоят «магические числа» типа if role_id in (6,7,8): ...,
	•	или напрямую проверяется role_id == 1 и т.п.,
заменить на:
	•	получение роли из UsersTelegaBot.role_id;
	•	вытягивание прав из roles_reference;
	•	проверку по флагам (например, if role.can_manage_users:).
	3.	В хендлерах /help, /start, /admin:
	•	сформировать разный список доступных команд на основе прав из roles_reference, а не по голым role_id.

⸻

Шаг 2. Логи действий админов admin_action_logs

Файлы: admin-репозиторий / admin-сервис, хендлеры админских команд.
	4.	Ввести единый AdminActionLogger (сервис или метод репозитория), который:
	•	принимает actor_id (ID в UsersTelegaBot), target_id (ID другого пользователя) и action (строка) + payload (dict → JSON);
	•	пишет запись в admin_action_logs.
	5.	Во всех командах, которые управляют пользователями/ролями:
	•	/approve — после успешного approve:
	•	action = 'approve', actor_id = superadmin/admin, target_id = одобренный.
	•	/make_admin, /make_superadmin, смена роли:
	•	action = 'make_admin' / 'make_superadmin' / 'change_role'.
	•	/block, /unblock:
	•	action = 'block' / 'unblock'.
	•	любые «просмотр списка админов / пользователей»:
	•	можно логировать lookup с target_id = NULL.
	6.	Везде, где раньше actor_id/target_id могли трактоваться как users.id (таблица Mango), перевести на UsersTelegaBot.id.

⸻

Шаг 3. Аналитический слой call_analytics

Файлы: репозиторий аналитики, сервис метрик, всё, что считает тяжёлую статистику.
	7.	Создать/вернуть CallAnalyticsRepository, который работает с таблицей call_analytics, а не с call_scores:
	•	методы выборки по:
	•	оператору + периоду;
	•	всем операторам за период;
	•	отдельному звонку по history_id / call_scores_id.
	8.	Реализовать ETL / синхронизацию:
	•	сервис или отдельная команда/cron, который:
	•	находит в call_scores звонки, которых ещё нет в call_analytics (по history_id или id);
	•	вставляет в call_analytics:
	•	call_date, is_target, outcome, call_category, call_score, talk_duration, operator_name/operator_extension и т.п.;
	•	ML-поля можно пока оставлять NULL.
	•	предусмотреть режим:
	•	«полное первичное заполнение»;
	•	«инкрементальное обновление» (только новые/изменившиеся).
	9.	Во всех сервисах, которые строили агрегаты напрямую из call_scores для дашбордов:
	•	переписать запросы на использование call_analytics как основного источника:
	•	группировка по оператору и дате;
	•	фильтры по is_target, outcome, call_category, call_score, talk_duration.

⸻

Шаг 4. Кеш дашбордов operator_dashboards

Файлы: dashboard_cache.py, сервис дашборда, хендлеры, вызывающие дашборд.
	10.	Вернуть реальную реализацию dashboard_cache.py:

	•	get_dashboard(operator, period_type, period_start):
	•	читает из operator_dashboards по (operator_name, period_type, period_start);
	•	save_dashboard(operator, period_type, period_start, metrics):
	•	делает upsert в operator_dashboards.

	11.	В сервисе дашборда (условно DashboardService):

	•	логика для оператора:
	1.	попробовать прочитать кеш из operator_dashboards;
	2.	если нет / устарел:
	•	посчитать агрегаты из call_analytics;
	•	сохранить в operator_dashboards через dashboard_cache;
	•	вернуть результат.
	•	логика для «все операторы»:
	•	либо аналогично кешировать сводные данные,
	•	либо пока считать на лету из call_analytics.

	12.	В телеграм-хендлерах команд:

	•	/weekly_quality, /operator_dashboard, /all_dashboard и т.п.:
	•	убрать прямые запросы к call_scores;
	•	вызывать только сервис дашборда → тот уже сам решает, брать из кеша или считать.

⸻

Шаг 5. Рекомендации operator_recommendations + reports

Файлы: analytics.py, reports.py, хендлеры отчётов.
	13.	В analytics.py вернуть функции:

	•	save_recommendations(operator_name, date, text, call_samples_count):
	•	upsert в operator_recommendations по (operator_name, report_date).
	•	get_recommendations(operator_name, date):
	•	чтение из operator_recommendations (последняя по дате ≤ указанной).

	14.	В reports.py/сервисе генерации отчётов:

	•	при формировании отчёта по оператору:
	1.	попробовать взять рекомендации из operator_recommendations;
	2.	если нет:
	•	сгенерировать через LLM;
	•	сохранить через save_recommendations;
	•	отдать текст пользователю.
	•	отчёты по клинике / отделу:
	•	продолжать хранить в reports (как сейчас), не смешивать с operator_recommendations.

	15.	В телеграм-хендлерах:

	•	для команд вроде /report, «отчёт по оператору»:
	•	включать в текст отчёта рекомендации, прочитанные из operator_recommendations (или только что сгенерированные).

⸻

Шаг 6. Привязка Telegram-пользователя к оператору

Файлы: репозиторий UsersTelegaBot, сервис пользовательских прав, хендлеры /my_stats, «мой дашборд» и т.п.
	16.	Везде, где нужен «мой операторский дашборд»:
	17.	по chat_id/user_id найти запись в UsersTelegaBot;
	18.	взять из неё:
	•	operator_name и/или extension (в зависимости от твоего выбора на шаге 0);
	19.	передавать это значение в DashboardService и CallAnalyticsRepository.
	20.	В командах «посмотреть дашборд по другим операторам» (если есть):

	•	список операторов строить:
	•	либо из UsersTelegaBot, где заполнены operator_name/extension и статус approved;
	•	либо из users (Mango) и уже маппить на UsersTelegaBot.

⸻

Шаг 7. Админ-панель и проверка команд

Файлы: AdminPanelHandler, AdminStatsHandler, help/start-хендлеры.
	18.	Пройтись по админ-хендлерам и:

	•	убрать все обращения к несуществующим полям/таблицам (старые ссылки на миграции);
	•	убедиться, что:
	•	права читаются через roles_reference;
	•	действия логируются в admin_action_logs;
	•	статистика берётся из call_analytics / operator_dashboards, а не из call_scores.

	19.	Обновить тексты /help и стартовые сообщения:

	•	явно отразить:
	•	какие команды доступны оператору;
	•	какие маркетологу;
	•	какие заведующему/руководству;
	•	какие только суперадмину/деву — всё на базе roles_reference.

⸻

Если хочешь, дальше могу разложить тот же план по слоям проекта с привязкой к файлам, вида:
«в db-слое поправить такие-то репозитории»,
«в сервисном — такие классы»,
«в telegram-слое — такие хендлеры», чтобы можно было идти чек-листом.

Сейчас по-честному: админка сделана как дамп команд, а не как система. Ниже — как её перестроить структурно, без кода.

⸻

1. Роли и кто что видит

Сразу жёстко: интерфейс должен строиться от роли, а не от списка всех возможных команд.

Используем твои роли (из roles_reference):
	•	1 — Оператор
	•	2 — Администратор (старшие админы, зав.рег)
	•	3 — Маркетолог
	•	4 — ЗавРег
	•	5 — Ст.админ
	•	6 — Руководство
	•	7 — SuperAdmin
	•	8 — Dev

Право can_manage_users и can_view_all_stats определяют доступ:
	•	Оператор — только свои отчёты / свои звонки.
	•	Руководство/Маркетолог — отчёты по всем, но без админки.
	•	Админ/Ст.админ/ЗавРег — отчёты + управление пользователями.
	•	SuperAdmin/Dev — всё, плюс системные/опасные вещи.

Все последующие пункты — с учётом этого.

⸻

2. Главный экран (reply-клавиатура)

Главная клавиатура должна быть короткой и говорить языком задач, не команд.

Для SuperAdmin / Dev / Админ / Ст.админ / ЗавРег

4 кнопки максимум:
	1.	«📊 Отчёты»
	2.	«🔍 Поиск звонка»
	3.	«👥 Пользователи» (только если can_manage_users = true)
	4.	«⚙️ Система» (только для 7–8 ролей: SuperAdmin/Dev)

Никаких /weekly_quality, /make_admin, /register на главной клавиатуре быть не должно.

Для Руководства / Маркетолога
	•	«📊 Отчёты»
	•	«🔍 Поиск звонка»

Без админки, без системных штук.

Для Оператора
	•	«📊 Моя статистика»
	•	«🔍 Мои звонки»

Всё. Админки нет вообще.

⸻

3. Раздел «📊 Отчёты» (inline-меню)

После нажатия кнопки «📊 Отчёты» бот всегда показывает одно сообщение с inline-кнопками, без простыни текста.

Для руководства / админов:

Inline-кнопки:
	1.	«Еженедельный отчёт»
→ по сути твой /weekly_quality.
	2.	«Отчёт за период»
→ твой /report с параметрами периода.
	3.	«Отчёт по оператору»
→ выбор оператора + период.
	4.	«Сводка по всем операторам»
→ агрегированный дашборд.
	5.	(опционально) «Экспорт»
→ если будешь делать выгрузки в файл.

Ни одной «сыраой» команды вида /weekly_quality пользователю показывать не нужно. Команды остаются для тебя и как backend-эндпоинты, но не как UX.

Для оператора:
	•	«Мой отчёт за неделю»
	•	«Мой отчёт за период»

То же самое, но без выбора оператора и без сводки по всем.

⸻

4. Раздел «🔍 Поиск звонка»

Сценарий — понятный и частый, отдельный раздел.

Inline-кнопки:
	1.	«По номеру телефона»
	2.	«По дате/интервалу»
	3.	«По оператору»
	4.	(опционально) «Последние N звонков» (быстрый доступ)

Дальше диалог пошаговый:
	•	выбрал «по номеру» → бот просит номер;
	•	выбрал «по дате» → бот просит дату/период;
	•	выбрал «по оператору» → сначала оператор, потом период.

Команда /call_lookup может существовать, но в UI ты её не пишешь — пользователь работает через кнопки.

⸻

5. Раздел «👥 Пользователи» (админка в чистом виде)

Это ядро админки. Сейчас у тебя это просто россыпь /make_admin, /approve, /admins и т.д. Нужно собрать в один сценарий.

После нажатия «👥 Пользователи» показываешь inline-меню:
	1.	«Ожидают одобрения»
	2.	«Список пользователей»
	3.	«Список админов»
	4.	«Изменить роль пользователя»

Никаких /make_superadmin на этом экране.

5.1. «Ожидают одобрения»
	•	Бот показывает список pending-юзеров (имя, роль, extension, дата).
	•	У каждого — inline-кнопки:
	•	«Одобрить»
	•	«Отклонить»

Каждое действие:
	•	логируется в admin_action_logs,
	•	меняет status в UsersTelegaBot,
	•	НЕ требует писать вручную /approve.

5.2. «Список пользователей»
	•	Пагинация: список из, скажем, 10 пользователей с ролями и статусом.
	•	Inline-кнопка «Подробнее» → карточка пользователя:
	•	ФИО
	•	роль
	•	статус (approved/blocked)
	•	extension/operator_name
	•	кнопки:
	•	«Изменить роль»
	•	«Заблокировать / Разблокировать»

Опасные операции внутри карточки, а не в главном меню.

5.3. «Список админов»
	•	Выводятся только пользователи с ролью can_manage_users = true (по roles_reference).
	•	Никаких операций по умолчанию,
	•	У каждого — кнопка «Изменить роль» (если текущий — SuperAdmin/Dev).

5.4. «Изменить роль пользователя»
	•	Сценарий:
	1.	бот просит: «Выберите пользователя» → список/поиск.
	2.	после выбора — показывает текущую роль и inline-кнопки:
	•	«Сделать админом»
	•	«Сделать руководством»
	•	«Сделать оператором»
	•	(и т.п. по твоей матрице ролей)
	3.	после выбора — доп.подтверждение:
	•	«Вы уверены, что хотите назначить [роль] пользователю [ФИО]?»
	•	«Да / Нет».

/make_admin / /make_superadmin в таком UX просто не нужны. Оставить максимум как скрытую команду только для Dev, без появления в help/кнопках.

⸻

6. Раздел «⚙️ Система» (только SuperAdmin/Dev)

Здесь только то, что может сломать всё, или нужно для отладки.

Inline-кнопки:
	1.	«Состояние бота»
	•	статусы: БД ок/не ок, очередь задач, последняя ошибка.
	2.	«Последние ошибки»
	•	вывести N последних логов/ошибок.
	3.	«Проверка доступа к Mango/БД»
	•	тестовый запрос и краткий результат.
	4.	(опционально) «Пересчитать кеш дашбордов»
Но такую кнопку лучше либо убрать вообще, либо запрятать глубже, либо оставлять только как скрытую команду.

Никаких действий по пользователям и ролям тут быть не должно.

⸻

7. /start и /help — перезагрузить

Сейчас у тебя /start и /help — две простыни.

/start

Должно быть очень коротким и разным по ролям.

Пример по смыслу (без формулировок):
	•	Оператор:
	•	«Вы авторизованы как оператор.
Вы можете: посмотреть свою статистику и найти свои звонки.
Используйте кнопки ниже.»
	•	Руководство:
	•	«Вы авторизованы как руководство.
Доступны: отчёты по всем операторам и поиск звонков.
Начните с раздела «Отчёты» или «Поиск звонка» ниже.»
	•	Админ / Ст.админ:
	•	«Вы авторизованы как администратор.
Доступны: отчёты, поиск звонков и управление пользователями.
Для настройки доступов — раздел «Пользователи».»
	•	SuperAdmin/Dev:
	•	«Вы авторизованы как superadmin.
Доступны: отчёты, поиск, управление ролями и системные функции.
Опасные операции находятся в разделах «Пользователи» и «Система».»

Никаких списков команд в /start.

/help

Вместо сплошного списка:
	1.	Разбить на блоки:
	•	«Отчёты»
	•	«Поиск звонков»
	•	«Пользователи»
	•	«Система» (если есть права)
	2.	Внутри каждого блока:
	•	2–4 ключевые команды/действия с объяснением
	•	Без мусора, без команд, которых пользователь не увидит в UI.
	3.	Для обычных ролей (оператор/рук-во) — help вообще может быть на 5–7 строк.

⸻

8. Что убрать/запретить визуально
	1.	/register — не показывать никому, кроме ещё не одобренных.
Одобренный пользователь не должен видеть эту команду нигде.
	2.	/make_admin, /make_superadmin, любые подобные:
	•	убрать из /help,
	•	убрать из клавиатуры,
	•	оставить максимум как скрытые команды для Dev и SuperAdmin - вынести в экран что только дев суперадмин и заврег могут назначать админов
	3.	Любые команды, которые ломают состояние системы (пересчёт кешей, тестовые запросы):
	•	только в «⚙️ Система»,
	•	только для 7–8 ролей,
	•	с подтверждением.

    все команды имеет право делать только супер админ или Dev - так как дев создатель продукта и иммеет дебагер, тестовые команды и возможность оповещать о технических работах всех пользователей 

⸻

9. Краткий жёсткий вывод
	•	Сейчас: ты вывалил API бота пользователю один в один.
	•	Нужно: сделать меню задач под конкретную роль:
	•	минимум шума,
	•	минимум кнопок,
	•	опасные операции глубоко и с подтверждением,
	•	команды в стиле /weekly_quality оставить только как технический слой.

Если нужно, следующим шагом могу разложить это по слоям проекта:
какие именно хендлеры должны остаться верхнеуровневыми (типа /start, /help, /admin), а какие спрятать и использовать только внутри inline-меню.