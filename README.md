# OperaBot — Telegram-бот для контроля качества операторов

## 🏗️ Архитектура

```
app/
├── config.py              # Конфигурация и переменные окружения
├── main.py                # Точка входа
├── logging_config.py      # WatchDog логирование
│
├── db/                    # Слой данных
│   ├── manager.py         # DatabaseManager (aiomysql pool)
│   └── repositories/      # Репозитории
│       ├── admin.py       # AdminRepository (UsersTelegaBot)
│       ├── operators.py   # OperatorRepository (users + Mango)
│       ├── lm_repository.py  # LM метрики
│       └── users.py       # UserRepository
│
├── ml/                    # ML слой
│   ├── models.py          # CallScorer, ChurnPredictor, UpsellRecommender
│   └── pipeline.py        # MLPipeline
│
├── services/              # Бизнес-логика
│   ├── lm_service.py      # Расчёт LM метрик
│   ├── metrics_service.py # Метрики качества
│   ├── dashboard_cache.py # Кеширование дашбордов
│   └── permissions.py     # Проверка прав
│
├── telegram/              # Telegram-хендлеры
│   ├── handlers/
│   │   ├── admin_panel.py    # /admin
│   │   ├── admin_users.py    # Управление пользователями
│   │   ├── admin_admins.py   # Управление администраторами
│   │   ├── admin_lm.py       # ML аналитика
│   │   └── admin_stats.py    # Статистика
│   └── bot.py             # Инициализация бота
│
└── workers/               # Фоновые задачи
    ├── lm_calculator_worker.py  # Расчёт LM метрик
    └── task_worker.py     # Очередь задач
```

## 🗄️ База данных

| Таблица | Назначение |
|---------|------------|
| `UsersTelegaBot` | Пользователи бота (роли, статусы) |
| `users` | Операторы Mango (extension, sip_id) |
| `call_history` | История звонков |
| `call_scores` | Оценки звонков |
| `lm_value` | ML метрики |
| `operator_dashboards` | Кеш дашбордов |
| `admin_action_logs` | Логи админ-действий |

## 🚀 Запуск

```bash
# Установка зависимостей
pip install -r requirements.txt

# Настройка окружения
cp .env.example .env
# Заполнить .env (см. секцию Переменные окружения ниже)

# Запуск бота
python -m app.main
```

## 📋 Переменные окружения

В репозитории есть `.env.example` — скопируйте и заполните.

Основные переменные:

```env
TELEGRAM_TOKEN=...
DB_HOST=localhost
DB_USER=operabot
DB_PASSWORD=...
DB_NAME=operabot
SUPREME_ADMIN_IDS=123456789
DEV_ADMIN_IDS=987654321
```

Redis (для стабильного разрешения длинных admin callback-ов)
- AdminCB поддерживает hashed-fallback (когда длина callback_data превышает лимит Telegram). Для надёжности mappings сохраняются в Redis.
- Добавьте в .env:

```env
REDIS_URL=redis://[:password@]hostname:6379/0
```

Примеры:
- Локально с Docker: `redis://localhost:6379/0`
- Managed (с паролем/TLS): `rediss://:password@redis.example.com:6380/0`

Быстрая проверка Redis:
- Локально через Docker:
  - docker run -d --name redis -p 6379:6379 redis:7-alpine
- Проверить из консоли:
  - redis-cli PING  -> PONG
- Проверить из Python (async):
  - python -c "import asyncio, os; from redis.asyncio import Redis; async def t(): r=Redis.from_url(os.environ['REDIS_URL']); await r.set('test','1',ex=60); print(await r.get('test')); await r.close(); asyncio.run(t())"

## 🔧 Docker-compose (локальная dev-конфигурация)

Пример сервиса redis, который можно добавить в `docker-compose.yml` для локальной разработки:

```yaml
services:
  redis:
    image: redis:7-alpine
    container_name: operabot_redis
    ports:
      - "6379:6379"
    restart: unless-stopped
```

После запуска `docker-compose up -d redis` установите в `.env`:
`REDIS_URL=redis://localhost:6379/0`

## 📦 Зависимости

Файл `requirements.txt` содержит все runtime-библиотеки. Мы добавили `redis>=4.5.0` для поддержки sync/async клиента redis-py.

```bash
pip install -r requirements.txt
```

Если вы используете контейнеры/CI — убедитесь, что в окружении CI задан REDIS_URL или тесты корректно мокают Redis.

## 🔧 Документация

- [Админ-панель](docs/ADMIN_PANEL.md)
- [NEW_админка — план и чек-лист](docs/NEW_админка.md) — обновлён: секция про Redis и рекомендации по развёртыванию.
- [LM метрики](docs/LM_СПРАВОЧНИК.md)
- [WatchDog логирование](docs/WATCHDOG_INTEGRATION.md)

## ✅ Что я сделал для стабильности admin-router

- Унифицирован формат callback_data для админки (adm:...).
- Единая точка входа: `admin_panel.handle_callback` ловит `^adm:` и делегирует feature-handlers.
- AdminCB теперь сохраняет длинные callback mappings в Redis (если REDIS_URL задан); fallback — in-memory cache.
- requirements и .env.example обновлены.

## 🛠️ Рекомендации по развёртыванию

1. Добавьте REDIS_URL в окружение прод/стейджа.
2. Установите зависимости (`pip install -r requirements.txt`) или пересоберите контейнер.
3. Прогоните тесты и smoke-check:
   - pytest -q (локально)
   - запустите бота в staging и вручную проверьте работу админ-меню (/admin) и длинных кнопок.
4. При миграции legacy-клавиатур — планируйте маршрут: сначала включаем LegacyAdapter, затем переводим генерацию кнопок на AdminCB.create.

---

Если хотите — я добавлю в docs/NEW_админка.md секцию «Как поднять Redis в dev» и короткий runbook для стейджа. Продолжить?
